# Data-Cleaning-Structural-Validation
A Python-based data cleaning project that transforms raw employee data into a clean, reliable, and analysis-ready dataset. It removes duplicates, handles missing values, standardizes formats, validates data types, and exports the cleaned data using Pandas and NumPy in Jupyter Notebook.
